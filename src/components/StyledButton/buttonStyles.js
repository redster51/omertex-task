import {withStyles} from "@material-ui/core";

const buttonStyles = withStyles({
    root: {
        textTransform: 'none',
        borderRadius: '2em',
        padding: '4px 30px',
        lineHeight: '1.6',
        minWidth: '115px',
    }
});

export default buttonStyles;
